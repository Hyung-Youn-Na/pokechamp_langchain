<!--
  PokéChamp 실험 분석 보고서 템플릿 (exp-030 패턴 일반화, IO/minimax/react 공용)
  사용법: cp exp_analysis/template.md docs/exp-{NNN}-{algo}-{model}-analysis.md
  작성 가이드: exp_analysis/ANALYSIS_MANUAL.md 8절.
  모든 <!-- ... --> 주석은 작성 완료 후 제거.
-->

# EXP-{NNN} ({algorithm} / {model}) 실험 분석

> 분석 일시: {YYYY-MM-DD}
> {EXP-ID}: {날짜}, {model}, {algorithm}, {N}전 vs {opponent}
> <!-- 비교 대상 EXP가 있으면 아래 라인 추가. 복수 가능. -->
> 비교: EXP-{NNN} ({algorithm}, {model}) — {변경점 한 줄}

<!-- 진행 중 실험(n_battles < 30)일 때만 아래 경고 박스 유지. N≥30 이면 삭제.
> ⚠️ N={N} < 30 통계 유의성 낮음. 완료(N=30) 후 재분석 필요.
-->

---

## 1. 실험 결과 비교

### 1.1 승률

<!-- 출처: experiment JSON summary.win_rate (전체) + 4.3 스니펫 (구간별) -->

| 구간 | EXP-{본 EXP} | EXP-{비교 EXP} | 변화 |
|------|--------------|----------------|------|
| **전체** | {Z}% ({W}/{N}) | {Z'}% ({W'}/{N'}) | ⬆/⬇ |
| 짧은 배틀 (<15턴) | {Z}% ({W}/{N}) | — | — |
| 중간 배틀 (15-24턴) | {Z}% ({W}/{N}) | — | — |
| 긴 배틀 (25+턴) | {Z}% ({W}/{N}) | — | — |

<!-- 진행 중 실험일 때만 아래 경고 유지.
> ⚠️ 위 구간별 승률은 표본이 작아 참고용.
-->

### 1.2 리소스 사용량

<!-- 출처: experiment JSON summary.avg_llm_calls / avg_*_tokens. 턴당 값은 avg_llm_calls / avg_turns -->

| 항목 | EXP-{본 EXP} | EXP-{비교 EXP} | 변화율 |
|------|--------------|----------------|--------|
| 배틀당 LLM 호출 | {n}건 | {n'}건 | {±x%} |
| 턴당 LLM 호출 | ~{n}건 | — | — |
| 배틀당 prompt 토큰 | {n} | {n'} | {±x%} |
| 배틀당 completion 토큰 | {n} | {n'} | {±x%} |
| 배틀당 총 토큰 | {n} | {n'} | {±x%} |

### 1.3 도구 사용 분포

<!--
  react 전용 섹션. 출처: 4.5 스니펫 (langgraph_tool_log.jsonl).
  IO / minimax 알고리즘이면 이 섹션 전체를 삭제하고, 대신 3.x 문제점에 "JSON 파싱 실패" 항목을 추가 (4.4 스니펫).
-->

| 도구 | EXP-{본 EXP} | 비율 | EXP-{비교 EXP} | 비율 | 변화 |
|------|--------------|------|----------------|------|------|
| `calculate_damage` | {n} | {%} | {n'} | {%} | ⬆/⬇ |
| `simulate_turn` | {n} | {%} | {n'} | {%} | ⬆/⬇ |
| `check_type_effectiveness` | {n} | {%} | {n'} | {%} | — |
| 기타 | {n} | {%} | {n'} | {%} | — |

---

## 2. 핵심 발견

### 2.1 결정적 차이

<!-- 1~2문단. 본 EXP 와 비교 EXP 의 가장 큰 차이를 먼저. 숫자로. -->

### 2.2 가설 / 인과 추론

<!-- 관찰된 차이가 *왜* 승률/리소스 변화로 이어졌는지 추론. 뷰어(4.1) 정성 분석 + 로그 증거(battle_tag, 턴) 로 뒷받침. -->

### 2.3 미해결 문제

<!-- 개선됐으나 완전히 해결되지 않은 항목. -->

---

## 3. 문제점 분석

<!-- 모든 문제는 🔴 P0 / 🟡 P1 / 🟢 P2 우선순위 부여. 각: 증거표 + 근본원인 + 개선방안.
     IO / minimax 인 경우 "JSON 파싱 실패 / 재시도" 를 P0 또는 P1 항목으로 여기에 추가 (4.4 스니펫). -->

### 🔴 P0-1: {문제 제목}

| 항목 | EXP-{본 EXP} | EXP-{비교 EXP} |
|------|--------------|----------------|
| {메트릭} | {값} | {값} |

**근본 원인:**
- ...

**개선 방안:**
- ...

### 🟡 P1-1: {문제 제목}

| 항목 | EXP-{본 EXP} | EXP-{비교 EXP} |
|------|--------------|----------------|
| {메트릭} | {값} | {값} |

**근본 원인:** ...
**개선 방안:** ...

### 🟢 P2-1: {문제 제목}

**내용:** ...

---

## 4. 이전 실험 대비 개선 현황

<!-- 출처: 비교 EXP 분석 보고서(docs/exp-NNN-*.md)의 문제점 + 본 EXP 데이터. 상태: ✅ 해결 / 🟡 완화 / 🔴 미해결 -->

| 이슈 | EXP-{이전} | EXP-{본 EXP} | 상태 |
|------|------------|--------------|------|
| {이슈명} | 🔴/🟡/✅ | 🔴/🟡/✅ | 해결/완화/미해결 |
| {이슈명} | 🔴/🟡/✅ | 🔴/🟡/✅ | 해결/완화/미해결 |

---

## 5. 권장 개선 우선순위

| 순위 | 문제 | 근거 | 난이도 | 예상 효과 |
|------|------|------|--------|-----------|
| 1 | {문제} | {데이터 근거} | 낮/중/높 | {정량 효과} |
| 2 | {문제} | {데이터 근거} | 낮/중/높 | {정량 효과} |
| 3 | {문제} | {데이터 근거} | 난/중/높 | {정량 효과} |

> 모든 권고는 범용 gen9ou 전략 관점이어야 함 (ANALYSIS_MANUAL.md 6.1·6.5절). abyssal 특화 공략은 제외.

---

## 6. 다음 단계

### 즉시
- [ ] {구체적 코드/설정 변경}
- [ ] {구체적 코드/설정 변경}

### EXP-{후속} 완료 후
<!-- 진행 중 실험을 본 보고서로 다룬 경우에만 유지 -->
- [ ] {N=30 결과 재확인 항목}

### 후속 실험
- [ ] {다음 ablation 아이디어} — baseline 대비 변수 1개만 변경
- [ ] 목표: 승률 {Z}%+, {구간} 승률 {Z}%+, {에러/리소스 목표}
